#!/usr/bin/env node

const sinon = require("sinon");
require("chai").should();

describe("split-cf-yaml", () => {
	it("should read the manifest.yml file", () => {

	})

	it("should write a new manifest.yml for each app listed", () => {

	});

	it("should accept an input_directory parameter", () => {

	})

	it("should accept an output_directory parameter", () => {

	})

	it("should read manifest.yml from provided input directory", () => {

	})

	it("should write manifests to provided output directory", () => {
		
	})
});